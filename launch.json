{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "pwa-chrome",
        "request": "launch",
        "name": "Launch HTML in Chrome",
        "file": "${workspaceFolder}/index.html",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }